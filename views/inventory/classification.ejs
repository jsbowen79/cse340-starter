<!-- Content Heading -->
<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/')} %>

<!-- Message Block--inactive -->
<%# messages() %>

<!-- Display Inventory Items -->
<%- grid %>